# Shared ACME HTTP-01 challenge handler. Every vhost includes this so
# certbot --webroot works without touching the server block per-domain.
location /.well-known/acme-challenge/ {
  root /var/www/certbot;
  default_type text/plain;
  try_files $uri =404;
}
