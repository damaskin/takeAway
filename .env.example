# ──────────────────────────────────────────────────────────────
# takeAway — local development environment variables
# Copy this file to .env and fill in real values for your machine.
# Never commit .env to git.
# ──────────────────────────────────────────────────────────────

NODE_ENV=development

# API
API_PORT=3000
API_HOST=0.0.0.0
API_GLOBAL_PREFIX=api

# Database (matches infra/docker-compose.yml — host port 55432 to avoid
# clashing with a native Postgres install on 5432)
DATABASE_URL=postgresql://takeaway:takeaway@localhost:55432/takeaway?schema=public

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_ACCESS_SECRET=change-me-in-prod-access
JWT_REFRESH_SECRET=change-me-in-prod-refresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# S3 / MinIO
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=takeaway-dev

# Mail (Mailhog in dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@takeaway.local

# 3rd-party (leave empty in local dev)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
MAPBOX_TOKEN=
MIXPANEL_TOKEN=
SENTRY_DSN=
TELEGRAM_BOT_TOKEN=

# ── Feature flags ─────────────────────────────────────────────────────────────
# Master switch for the M6 delivery module. Off by default — flip to `true`
# when ops is ready to launch delivery (enables dispatcher + rider UI + lets
# customers pick DELIVERY on checkout).
DELIVERY_ENABLED=false

# Phone + SMS OTP login. Off by default — Telegram Login Widget / Mini App
# init-data are the only auth paths in production. Flip to `true` to re-enable
# the phone form (requires Twilio credentials above).
AUTH_OTP_ENABLED=false

# Telegram Login Widget bot username (without @). Clients read this at build
# time to render the widget. Public value — token stays in TELEGRAM_BOT_TOKEN.
TELEGRAM_BOT_USERNAME=
