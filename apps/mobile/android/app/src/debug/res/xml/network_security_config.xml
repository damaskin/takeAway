<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds may reach a local API over plain HTTP (the emulator sees
     the host machine as 10.0.2.2). Never shipped. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
